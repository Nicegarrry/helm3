# Issue tracker

GitHub repository: `Nicegarrry/helm3` (private).

The live Map is issue #1. Read exact issue bodies with `gh issue view NUMBER --repo Nicegarrry/helm3 --json title,body,state`. Use native dependency links; local docs/map.md is only a projection. Approval #2 records the scoped first-wave decision. Worker changes use isolated branches and PRs; the coordinator integrates only reviewed, tested heads. Check fresh CI and PR head immediately before merge and read back the result. A merged PR alone does not close a capability issue: record its acceptance evidence and remaining gaps first.
